# Collaborators:

# Note: Do not change the name of the function!

def year_100(age):       # function names shouldn't start with numbers, thus the reversed name
    # add code here to calculate the year in which the user will be 100 year old
    # your calculation will be based on the parameter age, which is the age the user is now

    return year


if __name__ == '__main__':
    # Test your code with this first. Change the argument to try different values:
    print(year_100(25))
    # Now, prompt the user for their current age:

    # Print the value returned by calling the function, using the current age as a parameter:

